name: AI Publisher

on:
  schedule:
    - cron: '0 */4 * * *'   # يعمل كل 4 ساعات تلقائياً
  workflow_dispatch:        # يتيح لك تشغيله يدوياً بنقرة زر

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        # تمت إضافة مكتبة الترند ومكتبة البيئة هنا
        run: |
          npm install groq-sdk googleapis google-trends-api dotenv

      - name: Run publisher
        run: node index.js
        # تمرير المفاتيح السرية من إعدادات GitHub إلى الكود
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          BLOG_ID: ${{ secrets.BLOG_ID }}
          CLIENT_ID: ${{ secrets.CLIENT_ID }}
          CLIENT_SECRET: ${{ secrets.CLIENT_SECRET }}
          REFRESH_TOKEN: ${{ secrets.REFRESH_TOKEN }}
